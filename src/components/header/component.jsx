import { ScrollProgressBar } from "../scroll-progress-bar/component";

export const Header = () => {
  return (
    <div className="header">
      <h3>Header</h3>
      <ScrollProgressBar />
    </div>
  );
};
